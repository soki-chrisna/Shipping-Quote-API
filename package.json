{
  "name": "shipping-quote-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "test": "node --test",
    "check": "node scripts/check-syntax.js",
    "checkout": "node --env-file=.env examples/checkout-client.js"
  }
}
